# Copy to .env and adjust for your hardware.
# --- CPU laptop defaults (what ships) ---
WHISPER_MODEL=tiny
ASR_COMPUTE_TYPE=int8
NMT_BACKEND=opus
TTS_BACKEND=espeak

# --- GPU box (matches the proposal exactly) ---
# WHISPER_MODEL=large-v3
# ASR_COMPUTE_TYPE=float16
# NMT_BACKEND=seamless
# TTS_BACKEND=coqui
